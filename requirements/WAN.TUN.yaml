---
WAN.TUN:
  name: |
    WAN Tunnel

  categories:
    - !include WAN.TUN.VXLAN.yaml
    - !include WAN.TUN.L2.yaml

  requirements:
    1: |
      The RG Should support one or more tunnel protocol, such as
      Vxlan、GRE,L2TP
